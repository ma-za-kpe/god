# Agent graph implementations live here as Python modules.
# Each file defines a LangGraph StateGraph for one archetype.
# Phase 1: compile-time Python graphs (OwnedGraph CID stores state/params).
# Phase 3+: agent graphs fetched from IPFS and loaded dynamically.
