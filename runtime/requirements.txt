# Core agent framework
langgraph>=1.0.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-ollama>=0.2.0

# Web framework (runtime API)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Blockchain
web3>=7.0.0
eth-account>=0.13.0

# x402 micropayment bridge
x402>=0.1.0

# NATS event bus
nats-py>=2.7.0

# State persistence
redis>=5.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
sqlalchemy>=2.0.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# NVIDIA NeMo Agent Toolkit — primary for verifiable compute tasks and "AI Mode" cognition
# Local implementation: YES — fully supports local/offline models (Ollama, vLLM, HuggingFace local, TensorRT-LLM).
# No NVIDIA cloud/API key required for core inference/orchestration/guardrails.
# Switch from current (LangGraph + Ollama direct) is easy via config (see below).
# On Hetzner: Install NVIDIA drivers + CUDA on GPU server, run with --gpus, use NeMo's optimized backends for high perf.
# See docs/pivot-verifiable-compute.md "Preparing for Hetzner" section.
nemo-agent-toolkit>=0.1.0; python_version >= "3.11"  # optional — when set, the verifiable_compute handler and future agent graphs will use it instead of mocks/LangGraph
