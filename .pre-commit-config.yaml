# See https://pre-commit.com for usage. CI enforces via .github/workflows/pre-commit.yml
default_language_version:
  python: python3

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        exclude: ^contracts/(out|cache|broadcast)/
      - id: end-of-file-fixer
        exclude: ^contracts/(out|cache|broadcast)/
      - id: check-yaml
        args: [--unsafe]  # docker-compose uses custom tags
      - id: check-json
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=512]
      - id: detect-private-key
      - id: mixed-line-ending
        args: [--fix=no]
      - id: no-commit-to-branch
        args: [--branch, main, --branch, develop]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      - id: ruff
        args: [--fix]
        files: ^runtime/
      - id: ruff-format
        files: ^runtime/

  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.10.0.1
    hooks:
      - id: shellcheck
        args: [--severity=error]
        files: ^scripts/.*\.sh$

  - repo: https://github.com/hadolint/hadolint
    rev: v2.12.0
    hooks:
      - id: hadolint-docker
        args: [--failure-threshold, error]
        files: Dockerfile

  - repo: local
    hooks:
      - id: secret-literal-scan
        name: secret literal scan
        entry: python scripts/precommit-secret-scan.py
        language: system
        pass_filenames: true
        types: [text]
        stages: [pre-commit]
      - id: validation-pipeline
        name: validation pipeline
        entry: python scripts/validate-local.py
        language: system
        pass_filenames: false
        stages: [pre-commit]
